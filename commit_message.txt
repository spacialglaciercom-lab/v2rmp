🧹 Extract deep nesting into handle_file_selection

🎯 What: Extracted the `match browser.target_field` logic in `close_file_browser` into a new `handle_file_selection` helper function.
💡 Why: Improves readability by reducing deep nesting and separating concerns.
✅ Verification: Ran `cargo check`, `cargo clippy`, and `cargo test` to ensure functionality is preserved.
✨ Result: `close_file_browser` is much cleaner and `handle_file_selection` handles the individual field updates.
