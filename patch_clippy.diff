--- src/bin/rmpca-mcp-server.rs
+++ src/bin/rmpca-mcp-server.rs
@@ -1007,7 +1007,7 @@
             if let Some(s) = n.as_object() {
                 let id = s.get("id").and_then(|v| v.as_str()).unwrap_or("").to_string();
                 let label = s.get("label").and_then(|v| v.as_str())
-                    .unwrap_or_else(|| "")
+                    .unwrap_or("")
                     .to_string();
                 let lng = s.get("lng").and_then(|v| v.as_f64()).unwrap_or(0.0);
                 let lat = s.get("lat").and_then(|v| v.as_f64()).unwrap_or(0.0);
@@ -1618,36 +1618,26 @@
                 "solve_cw" => run_optimize(&req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_sweep" => run_optimize(&req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_nn" => run_optimize(&req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_two_opt" => run_optimize(&req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_or_opt" => run_optimize(&req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_cpp" => run_optimize(&req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_vrp" => run_vrp_optimize(req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_vrptw" => run_vrp_optimize(req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_cvrp" => run_vrp_optimize(req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 "solve_pdptw" => run_vrp_optimize(req)
                         .await
-                        .map_err(|e| anyhow::anyhow!("{e}"))
-                        .map(|v| v),
+                        .map_err(|e| anyhow::anyhow!("{e}")),
                 _ => return Err(anyhow::anyhow!("Unknown tool name: {}", name)),
             };
