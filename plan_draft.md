1. **Analyze and Map Constraints:** Extract the core invariants, boundary conditions, and state mutations from the `solve_cpp` function in `src/core/optimize.rs`, focusing specifically on the Hierholzer's algorithm implementation.
2. **Update Specification Document:** Overwrite the existing `formal_verification_spec.md` with a highly detailed Lean 4-compatible specification. This specification will include exact graph representations, pre/post-conditions, loop invariants, termination measures, and complexity proofs.
3. **Pre-commit Steps:** Run `pre_commit_instructions` to ensure proper testing, verification, review, and reflection are done.
4. **Submit:** Commit the updated specification and submit.
